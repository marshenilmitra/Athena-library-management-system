[pytest]
testpaths = tests
addopts = --cov=backend --cov-report=term-missing --ignore=tests/selenium_test
markers =
    unit: Unit tests
    boundary: Boundary value analysis tests
    integration: Integration tests
    security: Security vulnerability and RBAC tests
    property: Property-based testing
